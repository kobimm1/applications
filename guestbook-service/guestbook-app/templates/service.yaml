apiVersion: v1
kind: Service
metadata:
  name: {{ template "guestbook-app.fullname" . }}
  labels:
    app: {{ template "guestbook-app.name" . }}
    chart: {{ template "guestbook-app.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "guestbook-app.name" . }}
    release: {{ .Release.Name }}
