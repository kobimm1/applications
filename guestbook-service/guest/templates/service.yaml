apiVersion: v1
kind: Service
metadata:
  name: {{ template "guest.fullname" . }}
  labels:
    app: {{ template "guest.name" . }}
    chart: {{ template "guest.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "guest.name" . }}
    release: {{ .Release.Name }}
